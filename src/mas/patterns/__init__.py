"""Multi-agent topologies.

A pattern owns the turn-level control flow: which sub-agent holds the floor, how
tool results are routed back, when the system is ready to emit an outward
message. Patterns are swappable so that topologies can be compared on the same
benchmark tasks."""
